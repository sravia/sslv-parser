{
  "name": "serverless-sslv-parser",
  "author": "Aivars Sazanovics",
  "scripts": {
    "build": "tsc",
    "deploy": "rm -rf ./dist && npm run build && sls deploy",
    "lint": "eslint src/**/*.ts",
    "format": "eslint src/**/*.ts --fix"
  },
  "dependencies": {
    "@slack/webhook": "^6.1.0",
    "jsdom": "^16.2.13"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.84",
    "@types/aws-sdk": "^2.7.0",
    "@types/jsdom": "16.2.13",
    "@typescript-eslint/eslint-plugin": "^5.0.0",
    "@typescript-eslint/parser": "^5.0.0",
    "typescript": "^4.0.2"
  }
}
